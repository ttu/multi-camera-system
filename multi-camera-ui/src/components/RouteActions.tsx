import { RouteInfo } from "../types";

const RouteActions = (props: {route: RouteInfo}) => {
  return (
    <>
      <h1>Route Actions</h1>
      <div>Start Stop</div>
    </>
  );
};

export default RouteActions;
